# Database - PostgreSQL connection string (Coolify internal URL)
# El host será el nombre interno del contenedor de PostgreSQL en Coolify
DATABASE_URL="postgresql://postgres:PASSWORD@HOSTNAME:5432/postgres"

# NextAuth.js Configuration
# Genera un secreto con: openssl rand -base64 32
NEXTAUTH_SECRET="tu-secreto-super-seguro-aqui"

# La URL donde tu app está desplegada (sin slash al final)
# Para desarrollo local: http://localhost:3000
# Para producción: https://tu-dominio.com o http://IP:PUERTO
NEXTAUTH_URL="http://localhost:3000"
